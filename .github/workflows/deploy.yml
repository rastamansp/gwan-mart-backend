# ⚠️ WORKFLOW DESATIVADO.
#
# Este arquivo se chamava "Deploy to Production", disparava em todo push na main
# e executava `npm run deploy:prod` — que era build + docs + `echo 'Deploy
# realizado com sucesso!'`. Não publicava nada. Pior: dava sinal verde de deploy
# que nunca aconteceu, e não rodava teste algum.
#
# No ecossistema GWAN o deploy é feito exclusivamente pelo Portainer; CI serve
# para verificar. A verificação vive agora em ci.yml (build, lint, migrations em
# banco limpo e testes).
#
# O conteúdo foi esvaziado em vez de o arquivo ser removido apenas porque a
# remoção foi bloqueada pelo ambiente — apagar continua sendo o desfecho
# preferível. Sem `on:`, o GitHub Actions não agenda execução alguma.
