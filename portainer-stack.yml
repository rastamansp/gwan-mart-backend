# ⚠️ ARQUIVO DESATIVADO — NÃO É UMA STACK VÁLIDA.
#
# Até 2026-08-25 este arquivo continha a stack do **gwan-events-backend**,
# herdada por fork: nome de serviço `gwan-events-backend`, imagem
# `gwan-events-backend:latest`, CORS de `events.gwan.com.br` e MCP de
# `api-events.gwan.com.br`. Colá-lo no Portainer publicaria o app errado.
#
# A stack de produção do Gwan Mart é **docker-compose.production.yml**, na raiz
# deste repositório. É esse o arquivo a usar em Portainer → Stacks.
#
# O conteúdo antigo foi esvaziado em vez de o arquivo ser removido apenas porque
# a remoção foi bloqueada pelo ambiente; apagar o arquivo continua sendo o
# desfecho preferível. Vazio, ele falha alto no Portainer em vez de publicar
# silenciosamente outro aplicativo.
